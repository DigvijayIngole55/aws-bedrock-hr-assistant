# AWS Configuration
AWS_REGION=us-east-1
AWS_PROFILE=default

# Optional: Specific AWS Credentials (if not using AWS CLI)
# AWS_ACCESS_KEY_ID=your_access_key_here
# AWS_SECRET_ACCESS_KEY=your_secret_key_here

# Project Configuration
PROJECT_NAME=aws-bedrock-hr-assistant
AGENT_NAME=hr-assistant-function-def

# Database Configuration
DATABASE_NAME=employee_database.db

# Lambda Configuration  
LAMBDA_TIMEOUT=180
LAMBDA_MEMORY=128

# Logging Level
LOG_LEVEL=INFO